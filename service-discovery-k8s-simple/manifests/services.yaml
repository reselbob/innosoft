apiVersion: v1
kind: Service
metadata:
  namespace: istio-demo
  name: frontend
  labels:
    app: frontend
spec:
  selector:
    app: frontend
  ports:
  - port: 80
    targetPort: 3000
    name: http
  type: NodePort
---
apiVersion: v1
kind: Service
metadata:
  namespace: istio-demo
  name: business
  labels:
    app: business
spec:
  selector:
    app: business
  ports:
  - port: 80
    targetPort: 3000
    name: http